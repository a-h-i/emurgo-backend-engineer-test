{
  "name": "backend-engineer-test",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "run-docker": "docker-compose up -d --build",
    "schema:generate": "kysely-codegen --out-file ./src/db/schema.d.ts",
    "schema:migrate": "bun src/migrator/migrate.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.8"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.5",
    "fastify": "^4.28.1",
    "hono": "^4.10.7",
    "kysely": "^0.28.8",
    "kysely-codegen": "^0.19.0",
    "pg": "^8.12.0",
    "zod": "^4.1.13"
  }
}